/** Messages for StringsDisplayPage */

import { defineMessages } from 'react-intl';

export const scope = 'stringstore.containers.StringsDisplayPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'String Store Strings',
  },
});
